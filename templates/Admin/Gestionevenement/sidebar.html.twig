<aside class="admin-sidebar" style="
    width: var(--sidebar-width);
    background: var(--sidebar-bg);
    color: white;
    position: fixed;
    height: 100vh;
    left: 0;
    top: 0;
    z-index: 1000;
    display: flex;
    flex-direction: column;
    border-right: 1px solid rgba(255,255,255,0.1);
">

    <!-- Header -->
    <div style="padding: 25px; border-bottom: 1px solid rgba(255,255,255,0.1); text-align: center;">
        <h3 style="margin: 0; font-weight: 600; letter-spacing: 1px;">EVENTOPIA</h3>
    </div>

    <!-- Menu principal -->
    <nav style="flex: 1; padding: 20px 0; display: flex; flex-direction: column;">
        <!-- Events Dropdown -->
        <div class="dropdown-container">
            <div class="dropdown-toggle" 
                 onclick="toggleDropdown('events-dropdown')"
                 style="color: white; 
                        padding: 14px 25px; 
                        margin: 5px 15px; 
                        display: flex; 
                        align-items: center; 
                        justify-content: space-between;
                        text-decoration: none; 
                        border-radius: 6px;
                        cursor: pointer;
                        transition: all 0.3s;
                        {% if app.request.attributes.get('_route') starts with 'admin_evenement' %}background: rgba(255,255,255,0.15);{% endif %}">
                <div style="display: flex; align-items: center;">
                    <i class="fas fa-calendar-alt" style="margin-right: 15px; font-size: 1.1rem;"></i>
                    <span>Events</span>
                </div>
                <i class="fas fa-chevron-down dropdown-icon" style="transition: transform 0.3s;"></i>
            </div>
            
            <div id="events-dropdown" class="dropdown-content" 
                 style="display: {% if app.request.attributes.get('_route') starts with 'admin_evenement' %}block{% else %}none{% endif %};
                        margin-left: 30px;
                        margin-right: 15px;
                        background: rgba(0,0,0,0.2); 
                        border-radius: 0 0 6px 6px;
                        overflow: hidden;">
                <!-- List Events -->
                <a href="{{ path('admin_evenement_liste') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_evenement_liste' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-list" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>List Events</span>
                </a>
                
                <!-- Calendar -->
                <a href="{{ path('admin_evenement_calendar') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_evenement_calendar' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-calendar-alt" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Calendar</span>
                </a>

                <!-- Add Event -->
                <a href="{{ path('admin_evenement_ajouter') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_evenement_ajouter' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-plus-circle" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Add Event</span>
                </a>
            </div>
        </div>

        <!-- Users Dropdown -->
        <div class="dropdown-container">
            <div class="dropdown-toggle" 
                 onclick="toggleDropdown('users-dropdown')"
                 style="color: white; 
                        padding: 14px 25px; 
                        margin: 5px 15px; 
                        display: flex; 
                        align-items: center; 
                        justify-content: space-between;
                        text-decoration: none; 
                        border-radius: 6px;
                        cursor: pointer;
                        transition: all 0.3s;
                        {% if app.request.attributes.get('_route') starts with 'admin_utilisateur' or app.request.attributes.get('_route') starts with 'admin_Create' %}background: rgba(255,255,255,0.15);{% endif %}">
                <div style="display: flex; align-items: center;">
                    <i class="fas fa-users" style="margin-right: 15px; font-size: 1.1rem;"></i>
                    <span>Users</span>
                </div>
                <i class="fas fa-chevron-down dropdown-icon" style="transition: transform 0.3s;"></i>
            </div>
            
            <div id="users-dropdown" class="dropdown-content" 
                 style="display: {% if app.request.attributes.get('_route') starts with 'admin_utilisateur' or app.request.attributes.get('_route') starts with 'admin_Create' %}block{% else %}none{% endif %};
                        margin-left: 30px;
                        margin-right: 15px;
                        background: rgba(0,0,0,0.2); 
                        border-radius: 0 0 6px 6px;
                        overflow: hidden;">
                <!-- List Users -->
                <a href="{{ path('admin_utilisateur') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_utilisateur' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-list" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>List Users</span>
                </a>
                
                <!-- Add User -->
                <a href="{{ path('admin_Create_utilisateur') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_Create_utilisateur' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-user-plus" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Add User</span>
                </a>
            </div>
        </div>

        <!-- Materials Dropdown -->
        <div class="dropdown-container">
            <div class="dropdown-toggle" 
                 onclick="toggleDropdown('materials-dropdown')"
                 style="color: white; 
                        padding: 14px 25px; 
                        margin: 5px 15px; 
                        display: flex; 
                        align-items: center; 
                        justify-content: space-between;
                        text-decoration: none; 
                        border-radius: 6px;
                        cursor: pointer;
                        transition: all 0.3s;
                        {% if app.request.attributes.get('_route') starts with 'admin_gestionmateriel' or app.request.attributes.get('_route') starts with 'app_type_materiel' %}background: rgba(255,255,255,0.15);{% endif %}">
                <div style="display: flex; align-items: center;">
                    <i class="fas fa-box" style="margin-right: 15px; font-size: 1.1rem;"></i>
                    <span>Materials</span>
                </div>
                <i class="fas fa-chevron-down dropdown-icon" style="transition: transform 0.3s;"></i>
            </div>
            
            <div id="materials-dropdown" class="dropdown-content" 
                 style="display: {% if app.request.attributes.get('_route') starts with 'admin_gestionmateriel' or app.request.attributes.get('_route') starts with 'app_type_materiel' %}block{% else %}none{% endif %};
                        margin-left: 30px;
                        margin-right: 15px;
                        background: rgba(0,0,0,0.2); 
                        border-radius: 0 0 6px 6px;
                        overflow: hidden;">
                <!-- List Materials -->
                <a href="{{ path('admin_gestionmateriel_index') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_gestionmateriel_index' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-list" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>List Materials</span>
                </a>
                
                <!-- Material Types -->
                <a href="{{ path('app_type_materiel_index') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'app_type_materiel_index' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-tags" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Material Types</span>
                </a>

                <!-- Add Material -->
                <a href="{{ path('admin_gestionmateriel_new') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_gestionmateriel_new' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-plus-circle" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Add Material</span>
                </a>
            </div>
        </div>

        <!-- Espace Dropdown -->
        <div class="dropdown-container">
            <div class="dropdown-toggle" 
                 onclick="toggleDropdown('espace-dropdown')"
                 style="color: white; 
                        padding: 14px 25px; 
                        margin: 5px 15px; 
                        display: flex; 
                        align-items: center; 
                        justify-content: space-between;
                        text-decoration: none; 
                        border-radius: 6px;
                        cursor: pointer;
                        transition: all 0.3s;
                        {% if app.request.attributes.get('_route') starts with 'admin_espace' %}background: rgba(255,255,255,0.15);{% endif %}">
                <div style="display: flex; align-items: center;">
                    <i class="fas fa-map-marker-alt" style="margin-right: 15px; font-size: 1.1rem;"></i>
                    <span>Spaces</span>
                </div>
                <i class="fas fa-chevron-down dropdown-icon" style="transition: transform 0.3s;"></i>
            </div>
            
            <div id="espace-dropdown" class="dropdown-content" 
                 style="display: {% if app.request.attributes.get('_route') starts with 'admin_espace' %}block{% else %}none{% endif %};
                        margin-left: 30px;
                        margin-right: 15px;
                        background: rgba(0,0,0,0.2); 
                        border-radius: 0 0 6px 6px;
                        overflow: hidden;">
                <!-- List Spaces -->
                <a href="{{ path('admin_espace_list') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_espace_list' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-list" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>List Spaces</span>
                </a>
                
                <!-- Add Space -->
                <a href="{{ path('admin_espace_create') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'admin_espace_create' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-plus-circle" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Add Space</span>
                </a>
            </div>
        </div>

        <!-- Type Espace Dropdown -->
        <div class="dropdown-container">
            <div class="dropdown-toggle" 
                 onclick="toggleDropdown('type-espace-dropdown')"
                 style="color: white; 
                        padding: 14px 25px; 
                        margin: 5px 15px; 
                        display: flex; 
                        align-items: center; 
                        justify-content: space-between;
                        text-decoration: none; 
                        border-radius: 6px;
                        cursor: pointer;
                        transition: all 0.3s;
                        {% if app.request.attributes.get('_route') starts with 'app_type_espace' %}background: rgba(255,255,255,0.15);{% endif %}">
                <div style="display: flex; align-items: center;">
                    <i class="fas fa-tag" style="margin-right: 15px; font-size: 1.1rem;"></i>
                    <span>Space Types</span>
                </div>
                <i class="fas fa-chevron-down dropdown-icon" style="transition: transform 0.3s;"></i>
            </div>
            
            <div id="type-espace-dropdown" class="dropdown-content" 
                 style="display: {% if app.request.attributes.get('_route') starts with 'app_type_espace' %}block{% else %}none{% endif %};
                        margin-left: 30px;
                        margin-right: 15px;
                        background: rgba(0,0,0,0.2); 
                        border-radius: 0 0 6px 6px;
                        overflow: hidden;">
                <!-- List Space Types -->
                <a href="{{ path('app_type_espace_list') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'app_type_espace_list' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-list" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>List Space Types</span>
                </a>
                
                <!-- Add Space Type -->
                <a href="{{ path('app_type_espace_create') }}" 
                   style="color: white; 
                          padding: 12px 25px; 
                          display: flex; 
                          align-items: center; 
                          text-decoration: none; 
                          transition: all 0.3s;
                          {% if app.request.attributes.get('_route') == 'app_type_espace_create' %}background: rgba(255,255,255,0.15);{% endif %}"
                   class="menu-item">
                   <i class="fas fa-plus-circle" style="margin-right: 15px; font-size: 1rem;"></i>
                   <span>Add Space Type</span>
                </a>
            </div>
        </div>
    </nav>

    <!-- Section utilisateur -->
    <div style="padding: 20px; border-top: 1px solid rgba(255,255,255,0.1); background: rgba(0,0,0,0.1);">
        <div style="display: flex; align-items: center;">
            <div style="width: 45px; height: 45px; border-radius: 50%; background: rgba(255,255,255,0.1); 
                     display: flex; align-items: center; justify-content: center; margin-right: 15px;
                     border: 2px solid #bbe1fa;">
                <i class="fas fa-user"></i>
            </div>
            <div>
                <div style="font-weight: 500;">Administrator</div>
            </div>
        </div>
    </div>
</aside>

<script>
function toggleDropdown(dropdownId) {
    const dropdown = document.getElementById(dropdownId);
    const icon = dropdown.previousElementSibling.querySelector('.dropdown-icon');
    
    if (dropdown.style.display === 'none' || !dropdown.style.display) {
        dropdown.style.display = 'block';
        icon.style.transform = 'rotate(180deg)';
    } else {
        dropdown.style.display = 'none';
        icon.style.transform = 'rotate(0deg)';
    }
}

// Close dropdown when clicking outside
document.addEventListener('click', function(e) {
    if (!e.target.closest('.dropdown-container')) {
        document.querySelectorAll('.dropdown-content').forEach(dropdown => {
            dropdown.style.display = 'none';
        });
        document.querySelectorAll('.dropdown-icon').forEach(icon => {
            icon.style.transform = 'rotate(0deg)';
        });
    }
});

// Auto-expand dropdown if current page is in submenu
document.addEventListener('DOMContentLoaded', function() {
    const activeDropdowns = document.querySelectorAll('.dropdown-content[style*="display: block"]');
    activeDropdowns.forEach(dropdown => {
        const icon = dropdown.previousElementSibling.querySelector('.dropdown-icon');
        if (icon) {
            icon.style.transform = 'rotate(180deg)';
        }
    });
});
</script>

<style>
.dropdown-content {
    animation: fadeIn 0.2s ease-out;
}

.menu-item:hover {
    background: rgba(255,255,255,0.1) !important;
}

.dropdown-toggle:hover {
    background: rgba(255,255,255,0.1) !important;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(-10px); }
    to { opacity: 1; transform: translateY(0); }
}
</style>